local function create_js_consts()
    local text = 'const char * private_cweb_hydration_js_content = "'
    local files, size = dtw.list_files_recursively(HYDRATION_FILES)
    for i = 1, size do

    end
    text = text .. '"'
end

function Create_costs()

end
